%h1 Actualizar Desempeño
= link_to 'Atras', configurar_desempenos_path
= form_for :desempeno, url: configurar_desempeno_path(@desempeno), method: :patch do |f|
  - if @desempeno.errors.any?
    #error_explanation
      %h2
        = pluralize(@desempeno.errors.count, "error")
        No se admite campos vacios.
      %ul
        - @jugador.errors.full_messages.each do |msg|
          %li= msg
  %p
    = f.label :Titulo
    %br/
    = f.text_field :titulo
  %p
    = f.label :Descripcion
    %br/
    = f.text_area :descripcion
  %p
    = f.submit "Guardar"
